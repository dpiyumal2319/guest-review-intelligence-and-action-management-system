# Screenshot figures

Drop the following PNG/PDF screenshots here to replace the placeholders in `main.tex`.
If a file is absent, the report still compiles — a labelled placeholder box is shown instead.

- `dashboard.png` — the Dashboard page (KPI cards, sentiment/risk donuts, department bars).
- `issues.png` — the Issues page (Active issues list with risk, department, assignee).
- `issue-detail.png` — an Issue detail sheet (linked reviews + evidence snippets + event history).

Recommended width: ~1600px, captured at 100% zoom in a light theme for print legibility.
